# 既知課題（現行）

更新日: 2026-02-23

## 1. 編集機能
- 補間曲線編集は実装済みだが、複数キー同時編集やコピー/ペーストは未対応。
- カメラキー追加時の実値/補間スナップショット保存は実装済みだが、範囲編集UIは未実装。
- Property（表示/IK）トラック編集は未実装。
- VMDエクスポートは未実装。
- プロジェクトJSONは実装済みだが、外部ツール連携向けの仕様ドキュメント整備が不足。

## 2. タイムライン表現
- カメラ1行は同一フレーム列共有で、チャンネル別の独立キー編集は未対応。
- キー編集は追加/削除/1f移動が中心で、高度な範囲編集は未実装。

## 3. 物理
- 剛体モード 0/1/2 の運用仕様が未文書化。
- `disableBidirectionalTransformation` 相当切替の設計未完。
- 剛体/拘束デバッグ可視化は未実装。

## 4. 技術的注意
- `src/mmd-manager.ts` に文字コード由来の編集リスクがある環境がある。
- リポジトリはCRLF変換警告が出るため、差分確認時に行末変化へ注意。

## 5. 対応優先候補
- 1. Propertyトラック編集
- 2. VMDエクスポート
- 3. キー編集の範囲操作（複製/貼り付け/スケール）
- 4. 回転補間のMMD実機比較テスト自動化

## 2026-08-24 追記: 現行状況

上記の「更新日: 2026-02-23」時点の本文は、当時の既知課題スナップショットとして残す。現在の実装と矛盾する場合は、この追記と [MMD基本機能タスクチェックリスト](./mmd-basic-task-checklist.md) を優先する。

### 2026-02-23以降に完了した主な項目

- 複数キー選択、矩形選択、行・列選択、カテゴリ別全選択を実装した。
- 複数キーのcopy / paste / 反転paste、削除、移動、undo / redoを実装した。
- ボーン位置・回転、モーフ、カメラ各値の一括補正と、PMX体格差に基づく移動キー補正を実装した。
- モデル / カメラVMD書き出しβと、選択ボーンVPD書き出しβを実装した。ユーザー実機のMMD本家で基本読み込み成功まで確認した。
- 照明、影欄、重力のproject独自scene keyを登録・再生・シーク・保存・undo / redoへ接続した。
- PMX / PMDのalpha分類をbabylon-mmd標準評価へ寄せ、BMP / DDSの標準loader優先とcapability別fallbackを整理した。

### 現在残る編集課題

1. **Property（モデル表示 / IK ON・OFF）トラック編集**
   - project codec、編集データ、VMD入出力の土台はある。
   - タイムラインUI、登録・削除・copy / paste、再生・シーク時のruntime反映が未完成。
   - Propertyは連続曲線補間ではなく、直前キーの値を維持するステップ評価として扱う。
2. **時間軸構造操作**
   - 空フレーム挿入、フレーム列削除、指定位置以降の一括移動が未実装。
   - 選択キー操作とは分け、batch key editとしてundo / redo可能にする必要がある。
3. **キー登録補助**
   - 未変更時スキップ、上書き確認、複数対象の一括登録、オートキー対象制御が未実装。
4. **互換性・round-trip確認**
   - 回転補間のMMD本家比較、補正済みVMDの読み戻し、音声・カメラ・照明・PostFXを含むproject総合round-tripが残る。
5. **再生速度と物理**
   - 再生速度切り替え、速度変更 / シーク時の物理整合、物理モード比較、拘束デバッグ表示が残る。

### 現在残るタイムライン表現上の制約

- カメラは1行の共通フレーム列として扱い、位置・回転・距離・FoVをチャンネル別の独立キーにはしていない。
- Accessoryの情報欄と下パネルは整理済みだが、Accessory独自タイムライントラックとの対象選択同期は未完成。
- Effect keyは安定した少数パラメータへ限定したExperimental PoCの段階にも未着手である。

### 優先度

汎用3D形式の追加より、次を優先する。

1. project保存 / 読み込みの総合round-trip確認
2. Property（表示 / IK）のステップキー編集とプレビュー
3. 空フレーム挿入 / フレーム列削除
4. キー登録補助とオートキー対象制御
5. 回転補間のMMD本家互換確認
6. 再生速度切り替えと物理整合確認

## 2026-08-24 追記: Property / 時間軸編集 / 登録補助の実装反映

同日追記の「現在残る編集課題」1〜4のうち、次を実装・自動確認した。

- Property（表示 / IK）のタイムライン行、登録、削除、copy / paste、Undo / Redo、ステップ式seek / preview
- 情報欄のIK ON/OFF UI
- 空フレーム挿入 / フレーム列削除のbatch Command
- 登録時の未変更スキップ、手動上書き確認、複数ボーン / モーフの一括登録
- Auto Key対象の `すべて / 選択ボーン / モーフ / カメラ` 切替
- PMX、VMD path、無音WAV、Property、camera animation、照明、重力、主要PostFXを含むElectron project round-trip E2E

残るものは、全sceneと音声を同時にずらすリップル編集、実データによる音声同期の聴感確認、DoF / LUT / Fogを含めた同時手動確認、回転補間のMMD本家互換確認である。詳細は [Propertyキー・時間軸編集・キー登録・project round-trip 実装メモ](./property-frame-edit-registration-roundtrip-2026-08-24.md) を参照する。
