# エフェクト形状確認用fixture

既存の自作tofu生成器を再利用し、しきい値の上下を比較できる暗い材質を設定する。
オフセット影用には、既存tofuの三角マーカーをカメラ側へ移し、箱の前面との深度差0.8を作る。最小/最大深度内に入る境界を固定し、キー強度とオフセットによる描画変化を比較する。
第三者モデル・テクスチャは含まない。E2Eではgenerate.mjsからtest結果ディレクトリへPMXを生成し、fixture読込hookまたは通常のproject読込UIで供給し、キー操作と保存読込後の状態はGUIで確認する。
